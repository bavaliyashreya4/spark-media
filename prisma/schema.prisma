generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Contact {
  id          String   @id @default(cuid())
  name        String
  email       String
  projectType String
  message     String
  createdAt   DateTime @default(now())
}
